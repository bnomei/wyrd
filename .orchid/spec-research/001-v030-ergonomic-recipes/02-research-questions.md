# Current-State Questions

- Which current authoring surface already provides validated static graph syntax?
- Where do cookbook and Bevy examples repeat graph construction, runtime binding, and string port resolution?
- Which crate boundaries and feature constraints protect `no_std`, signal-i32, and Bevy's f32-only integration?
- Which tests and CI jobs define compatibility and release acceptance?
- Which API layers are needed for fixed graphs, generated graphs, generic hosts, Bevy, and machine tooling?
