name: ci

on:
  push:
    branches: [main, master]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  dual-signal:
    name: dual-signal (${{ matrix.signal }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - signal: signal-f32
            features: "std,signal-f32"
          - signal: signal-i32
            features: "std,signal-i32"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Test core/graph/runtime (${{ matrix.signal }})
        run: |
          cargo test -p wyrd-core -p wyrd-graph -p wyrd-runtime \
            --no-default-features \
            --features "${{ matrix.features }}"
      - name: Test graph serde-ron (${{ matrix.signal }})
        run: |
          cargo test -p wyrd-graph \
            --no-default-features \
            --features "${{ matrix.features }},serde-ron"
      - name: Test graph serde-json (${{ matrix.signal }})
        run: |
          cargo test -p wyrd-graph \
            --no-default-features \
            --features "${{ matrix.features }},serde-json"
      - name: Test graph serde-ron + serde-json cross-codec (${{ matrix.signal }})
        run: |
          cargo test -p wyrd-graph \
            --no-default-features \
            --features "${{ matrix.features }},serde-ron,serde-json"

  bevy-f32:
    name: bevy (signal-f32 only)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Test wyrd-bevy
        run: cargo test -p wyrd-bevy
      - name: Build and_door example
        run: cargo build -p wyrd-bevy --example and_door

  no-std-i32:
    name: no_std core signal-i32
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Check wyrd-core no_std + i32
        run: |
          cargo check -p wyrd-core \
            --no-default-features \
            --features "alloc,signal-i32"

  coverage:
    name: coverage (llvm-cov 100%)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - name: Coverage f32 core suite (fail-under 100)
        run: |
          cargo llvm-cov --workspace --exclude wyrd-bevy \
            --lcov --output-path lcov-f32.info \
            --fail-under-lines 100
      - name: Coverage i32 core suite (fail-under 100)
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov -p wyrd-core -p wyrd-graph -p wyrd-runtime \
            --no-default-features --features "std,signal-i32" \
            --lcov --output-path lcov-i32.info \
            --fail-under-lines 100
      - name: Coverage serde-ron f32 (fail-under 100)
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov -p wyrd-graph \
            --no-default-features --features "std,signal-f32,serde-ron" \
            --fail-under-lines 100 --summary-only
      - name: Coverage serde-json f32 (fail-under 100)
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov -p wyrd-graph \
            --no-default-features --features "std,signal-f32,serde-json" \
            --fail-under-lines 100 --summary-only
      - name: Coverage bevy f32 (fail-under 100)
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov -p wyrd-bevy \
            --lcov --output-path lcov-bevy.info \
            --fail-under-lines 100
      - name: Upload lcov
        uses: actions/upload-artifact@v4
        with:
          name: lcov
          path: |
            lcov-f32.info
            lcov-i32.info
            lcov-bevy.info
