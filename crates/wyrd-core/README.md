# wyrd-core

Engine-neutral types for [Wyrd](https://github.com/bnomei/wyrd): `Signal`, dense ids, closed port tables, `KnotKind`.

- `no_std` + `alloc` via `no-std-compat`
- Features: `signal-f32` (default) or `signal-i32`, optional `std`
