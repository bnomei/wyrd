# wyrd-graph

Authoring surface: `Weave`, builder, validate (DAG, fan-in, ports, budgets),
and **Pattern expand-at-load** (`include` / `merge_expanded`).

Validate after stamping patterns. Runtime never sees Pattern.
