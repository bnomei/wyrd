+++
id = "T002"
title = "Add pattern macro and migrate Pattern recipe"
status = "done"
scope = ["crates/wyrd-for-games/src/authoring/", "crates/wyrd-for-games/src/lib.rs", "crates/wyrd-for-games/src/runtime_impl/cookbook/tier_b.rs", "crates/wyrd-for-games/tests/"]
depends = ["T001"]
covers = ["R002"]
verification_mode = "required"
verification_status = "passed"
worker_reasoning_effort = "high"
worker_model = "sol"
test_strategy = "red-green-refactor"
slice = "normal"
reuse_targets = ["crates/wyrd-for-games/src/authoring/macros.rs", "crates/wyrd-for-games/src/authoring/pattern.rs"]
read_allowlist = ["crates/wyrd-for-games/src/authoring/macros.rs", "crates/wyrd-for-games/src/authoring/pattern.rs", "crates/wyrd-for-games/tests/weave_macro.rs", "crates/wyrd-for-games/src/runtime_impl/cookbook/tier_b.rs"]
bundle_with = []
commit = ""
commit_review = ""
implemented_by = "worker:/root/t002_pattern"
verified_by = "validator"
completed_at = "2026-07-11T22:37:56+01:00"
last_lease_id = "l_87c0d3700094"
+++

## Context

Add a dedicated exported `pattern!` macro. Its grammar is `id`, optional numeric path, `knots`, `exports { input name = knot.port; output name = knot.port; }`, and `threads`. It must lower to existing `PatternDef`/`Pattern` validation, preserve expression-once/source-order guarantees, and never weaken builder/pattern validation. Convert B01 to it.

## DoD

- `pattern!` returns `Result<Pattern, BuildError>` with documented public syntax.
- Macro supports aliases and valid exports; invalid and duplicate exports retain contextual errors.
- Tier B monostable uses the macro.
- Runtime, macro parity, and compile-fail diagnostics are covered.

## Validation

- `cargo test -p wyrd-for-games --test weave_macro`
- `cargo test -p wyrd-for-games --test weave_macro_ui`
- `cargo test -p wyrd-for-games --test patterns_cookbook`

## Escalate If

Pattern validation requires an error type change that would break v0.2 callers.
