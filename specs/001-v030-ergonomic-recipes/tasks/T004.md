+++
id = "T004"
title = "Add Scenario harness and typed cookbook bindings"
status = "todo"
scope = ["crates/wyrd-for-games/src/runtime_impl/", "crates/wyrd-for-games/tests/"]
depends = ["T003"]
covers = ["R003", "R004"]
verification_mode = "required"
verification_status = "pending"
worker_reasoning_effort = "high"
worker_model = "sol"
test_strategy = "red-green-refactor"
slice = "normal"
reuse_targets = ["crates/wyrd-for-games/src/runtime_impl/cookbook/helpers.rs", "crates/wyrd-for-games/src/runtime_impl/host.rs"]
read_allowlist = ["crates/wyrd-for-games/src/runtime_impl/cookbook/helpers.rs", "crates/wyrd-for-games/src/runtime_impl/cookbook/", "crates/wyrd-for-games/src/runtime_impl/host.rs", "crates/wyrd-for-games/src/runtime_impl/mod.rs", "crates/wyrd-for-games/src/runtime_impl/recipe.rs"]
bundle_with = []
commit = ""
commit_review = ""
+++

## Context

Add a public closure-scoped Scenario API for bound recipes: typed frame writes, loom/tick driving, truthy/value/emit expectations. Convert cookbook runners to typed Recipe ports and Scenario where it improves clarity; preserve exact public runners and assertions. Keep direct Runtime and ScriptedHost use valid for advanced callers.

## DoD

- Cookbook runners stop repeating untyped string lookup and raw frame plumbing.
- Frame closures cannot use ports from another recipe instance.
- Scenario errors remain contextual and no runner behavior changes.

## Validation

- `cargo test -p wyrd-for-games --test tutorial_ladder`
- Focused scenario tests covering missing output, falsey values, emits, and multiple frames.

## Escalate If

Scenario ownership would require mutable aliasing or erase Runtime handle safety.
