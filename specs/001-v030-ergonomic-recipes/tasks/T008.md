+++
id = "T008"
title = "Prepare v0.3.0 release metadata and documentation"
status = "todo"
scope = ["Cargo.toml", "Cargo.lock", "crates/wyrd-for-games-bevy/Cargo.toml", "README.md", "crates/wyrd-for-games/README.md", "crates/wyrd-for-games-bevy/README.md", "CHANGELOG.md", ".github/workflows/publish.yml", ".github/workflows/ci.yml"]
depends = ["T007"]
covers = ["R008"]
verification_mode = "required"
verification_status = "pending"
worker_reasoning_effort = "high"
worker_model = "sol"
test_strategy = "existing-tests-only"
slice = "normal"
reuse_targets = ["README.md", "CHANGELOG.md", ".github/workflows/publish.yml"]
read_allowlist = ["Cargo.toml", "crates/wyrd-for-games-bevy/Cargo.toml", "README.md", "crates/wyrd-for-games/README.md", "crates/wyrd-for-games-bevy/README.md", "CHANGELOG.md", ".github/workflows/publish.yml", ".github/workflows/ci.yml"]
bundle_with = []
commit = ""
commit_review = ""
+++

## Context

Prepare but do not publish/tag v0.3.0. Bump workspace/internal requirements and every visible dependency snippet. Document the authoring choice hierarchy, generic Recipe use, Scenario, Composer, manifest/schema feature, and Bevy plugin boundary. Move release notes into a 0.3.0 changelog section. Update publish workflow text and crates.io polling version.

## DoD

- All metadata and documentation consistently say 0.3.0.
- Docs describe only implemented API behavior.
- Publish workflow validates the correct version while remaining manual.

## Validation

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- Run CI-equivalent core feature, no_std, Bevy, package, and rustdoc gates; do not publish.

## Escalate If

Release metadata requires a publish/tag or package validation reveals files outside the intended package surface.
